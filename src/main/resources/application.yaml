spring:
  datasource:
    # База данных разворачивается в отдельном контейнере.
    # TODO: Решить вопрос с адресами базы данных для различных ситуаций запуска.
    url: 'jdbc:mariadb://mariadb:3306/web-chat'
    username: 'arkham'
    password: 'secret'
    driver-class-name: 'org.mariadb.jdbc.Driver'
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: 'update'
    properties:
      hibernate:
        dialect: 'org.hibernate.dialect.MariaDBDialect'
